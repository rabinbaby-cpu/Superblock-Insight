import { useState } from "react";
import { useLocation } from "wouter";
import { ArrowRight, CheckCircle2, Eye, EyeOff, Loader2, LockKeyhole, MessageCircleMore, ShieldCheck, Sparkles, Zap } from "lucide-react";
import { BrandMark } from "@/components/dashboard-ui";
import { Button } from "@/components/ui/button";
import { Checkbox } from "@/components/ui/checkbox";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { toast } from "sonner";

export default function Login(){const [,navigate]=useLocation();const [email,setEmail]=useState("anika@superblock.chat");const [password,setPassword]=useState("superblock-demo");const [show,setShow]=useState(false);const [loading,setLoading]=useState(false);const [error,setError]=useState("");const submit=(e:React.FormEvent)=>{e.preventDefault();setError("");if(!email.includes("@")){setError("Enter a valid work email address.");return}if(password.length<6){setError("Password must contain at least 6 characters.");return}setLoading(true);setTimeout(()=>{setLoading(false);toast.success("Welcome back, Anika");navigate("/analytics")},900)};return <div className="grid min-h-screen bg-background lg:grid-cols-[1.05fr_.95fr]"><section className="relative hidden overflow-hidden bg-[#0d0f0e] p-12 text-white lg:flex lg:flex-col"><div className="absolute inset-0 login-grid opacity-35"/><div className="relative z-10"><BrandMark/><div className="mt-24 max-w-xl"><div className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-[10px] text-white/70"><span className="size-1.5 rounded-full bg-[#b7f34a]"/>Customer operations, unified</div><h1 className="mt-6 text-[48px] font-semibold leading-[1.04] tracking-[-.055em]">Every customer signal.<br/><span className="text-[#b7f34a]">One operating view.</span></h1><p className="mt-5 max-w-lg text-sm leading-6 text-white/55">Superblock Analytics Studio connects customer health, communication usage, subscriptions, billing, meetings, and account context for fast internal decisions.</p></div><div className="mt-16 grid max-w-xl grid-cols-3 gap-3">{[["1,284","Active customers"],["24.8M","Messages / month"],["₹2.48Cr","Recurring revenue"]].map(([v,l])=><div className="rounded-xl border border-white/10 bg-white/[.035] p-4" key={l}><div className="font-tabular text-xl font-semibold">{v}</div><div className="mt-1 text-[10px] text-white/45">{l}</div></div>)}</div></div><div className="relative z-10 mt-auto flex items-center justify-between text-[10px] text-white/35"><span>Internal operations workspace</span><span className="flex items-center gap-1.5"><ShieldCheck className="size-3"/>Enterprise controls</span></div></section><section className="flex items-center justify-center p-5 sm:p-10"><div className="w-full max-w-[390px]"><div className="mb-10 lg:hidden"><BrandMark/></div><div><div className="text-[10px] font-semibold uppercase tracking-[.16em] text-muted-foreground">Secure workspace</div><h2 className="mt-2 text-3xl font-semibold tracking-[-.045em]">Welcome back</h2><p className="mt-2 text-sm text-muted-foreground">Sign in to Superblock Analytics Studio.</p></div><form onSubmit={submit} className="mt-8 space-y-4"><div><Label htmlFor="email" className="text-xs">Work email</Label><Input id="email" className="mt-1.5 h-10" type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@company.com" aria-invalid={!!error}/></div><div><div className="flex items-center justify-between"><Label htmlFor="password" className="text-xs">Password</Label><button type="button" className="text-[10px] font-medium text-muted-foreground hover:text-foreground" onClick={()=>toast.info("Password reset link sent",{description:"Mock interaction only."})}>Forgot password?</button></div><div className="relative mt-1.5"><Input id="password" className="h-10 pr-10" type={show?"text":"password"} value={password} onChange={e=>setPassword(e.target.value)} aria-invalid={!!error}/><button type="button" onClick={()=>setShow(!show)} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground hover:text-foreground">{show?<EyeOff className="size-4"/>:<Eye className="size-4"/>}</button></div></div>{error&&<div className="rounded-lg border border-rose-200 bg-rose-50 p-3 text-[11px] text-rose-700 dark:border-rose-950 dark:bg-rose-950/30 dark:text-rose-300">{error}</div>}<div className="flex items-center gap-2"><Checkbox id="remember" defaultChecked/><Label htmlFor="remember" className="text-[11px] font-normal text-muted-foreground">Remember me on this device</Label></div><Button className="h-10 w-full" disabled={loading}>{loading?<Loader2 className="size-4 animate-spin"/>:<LockKeyhole className="size-4"/>}{loading?"Signing in…":"Sign in"}{!loading&&<ArrowRight className="ml-auto size-4"/>}</Button></form><div className="mt-8 border-t pt-6"><div className="flex items-center justify-center gap-2 text-[10px] text-muted-foreground"><CheckCircle2 className="size-3 text-emerald-500"/>Mock authentication · Any valid email and password works</div></div></div></section></div>}
